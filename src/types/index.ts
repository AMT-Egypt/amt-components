import { ReactNode } from "react";

export interface goToTopProps {
    Icon: ReactNode,
    paddingSize?: string,
    backgroundColor?: string,
    borderRadius?: string,
    border?: string,
    bottom?: string,
    right?: string,
    left?: string
}